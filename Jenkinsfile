pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                echo 'Checkout git repo'
            }
        }
        stage('Build') {
            steps {
                echo 'Build Stage'
            }
        }
    }
}
